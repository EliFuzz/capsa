[package]
name = "core-regex"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"
doctest = false

[lints]
workspace = true

[features]
default = []
gen = ["dep:bindgen"]

[build-dependencies]
bindgen = { version = "0.72.1", optional = true }
